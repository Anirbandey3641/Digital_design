# 16×16 RAM Design Using Verilog

A 16×16 Random Access Memory (RAM) designed and verified using Verilog HDL.

This project implements a memory with 16 locations, where each location stores 16 bits of data. The design includes read and write operations and is verified using a self-checking testbench.
